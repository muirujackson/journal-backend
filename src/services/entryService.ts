// import { createEntry, getEntriesByUser, getEntryById, updateEntry, deleteEntry, Entry } from '../models/entryModel';

// export const addEntry = async (entry: Entry) => {
//   const newEntry = await createEntry(entry);
//   return newEntry;
// };

// export const getUserEntries = async (userId: number) => {
//   const entries = await getEntriesByUser(userId);
//   return entries;
// };

// export const getEntry = async (entryId: number) => {
//   const entry = await getEntryById(entryId);
//   return entry;
// };

// export const editEntry = async (entry: Entry) => {
//   const updatedEntry = await updateEntry(entry);
//   return updatedEntry;
// };

// export const removeEntry = async (entryId: number) => {
//   const deletedEntry = await deleteEntry(entryId);
//   return deletedEntry;
// };